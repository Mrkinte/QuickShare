<ui:FluentWindow
    x:Class="QuickShare.Views.Windows.ShareEditWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:QuickShare.Helpers"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:QuickShare.Views.Windows"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="{DynamicResource ShareEdit_Text_Title}"
    Width="800"
    Height="450"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <helpers:InvertedBooleanToVisibilityConverter x:Key="InvertedBooleanToVisibilityConverter" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ui:TitleBar
            x:Name="TitleBar"
            Title="{DynamicResource ShareEdit_Text_Title}"
            Grid.Row="0"
            CloseWindowByDoubleClickOnIcon="True">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/app-icon-512.png" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>
        <Grid Grid.Row="1" Margin="24,0,24,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal">
                <ui:TextBlock VerticalAlignment="Center" Text="{DynamicResource ShareEdit_Text_Description}" />
                <ui:TextBox
                    MinWidth="180"
                    Margin="12,0,0,0"
                    ClearButtonEnabled="False"
                    Text="{Binding Description, Mode=TwoWay}">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="TextChanged">
                            <i:InvokeCommandAction Command="{Binding ConfirmModifyEnableCommand}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ui:TextBox>
                <ui:TextBlock
                    Margin="24,0,0,0"
                    VerticalAlignment="Center"
                    Text="{DynamicResource ShareEdit_Text_VerifyCode}" />
                <ui:TextBox
                    Width="80"
                    Margin="12,0,0,0"
                    ClearButtonEnabled="False"
                    MaxLength="4"
                    Text="{Binding VerifyCode, Mode=TwoWay}"
                    TextAlignment="Center">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="TextChanged">
                            <i:InvokeCommandAction Command="{Binding ConfirmModifyEnableCommand}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ui:TextBox>
                <ui:Button
                    Margin="8,0,0,0"
                    Padding="8"
                    Background="Transparent"
                    BorderThickness="0"
                    Command="{Binding RandomAccessCodeCommand}"
                    Icon="{ui:SymbolIcon Symbol=ArrowSync24}" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:Button
                    Margin="12,0,0,0"
                    Appearance="Primary"
                    Command="{Binding AddFilesCommand}"
                    Content="{DynamicResource ShareEdit_Button_AddFiles}" />
                <ui:Button
                    Margin="12,0,0,0"
                    Appearance="{Binding ModifyBtnAppearance}"
                    Command="{Binding ConfirmModifyCommand}"
                    Content="{DynamicResource ShareEdit_Button_ConfirmModify}"
                    IsEnabled="{Binding IsModified}" />
            </StackPanel>
        </Grid>
        <ui:DataGrid
            Grid.Row="2"
            Margin="24"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            ItemsSource="{Binding ShareFiles, Mode=OneWay}">
            <!--  Style  -->
            <DataGrid.ColumnHeaderStyle>
                <Style BasedOn="{StaticResource DefaultDataGridColumnHeaderStyle}" TargetType="DataGridColumnHeader">
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.CellStyle>
                <Style BasedOn="{StaticResource DefaultDataGridCellStyle}" TargetType="DataGridCell">
                    <Setter Property="HorizontalAlignment" Value="Center" />
                </Style>
            </DataGrid.CellStyle>

            <ui:DataGrid.Columns>
                <DataGridTextColumn
                    Width="*"
                    Binding="{Binding Path, Mode=OneWay}"
                    Header="{DynamicResource ShareEdit_DataGrid_File}"
                    IsReadOnly="True">
                    <DataGridTextColumn.CellStyle>
                        <Style BasedOn="{StaticResource DefaultDataGridCellStyle}" TargetType="DataGridCell">
                            <Setter Property="HorizontalAlignment" Value="Left" />
                        </Style>
                    </DataGridTextColumn.CellStyle>
                </DataGridTextColumn>
                <DataGridTextColumn
                    Width="Auto"
                    Binding="{Binding DownloadCount, Mode=OneWay}"
                    Header="{DynamicResource ShareEdit_DataGrid_DownloadCount}"
                    IsReadOnly="True" />
                <DataGridTemplateColumn Width="Auto" Header="{DynamicResource ShareEdit_DataGrid_Status}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <ui:TextBlock
                                    Foreground="Green"
                                    Text="{DynamicResource ShareEdit_Text_ValidStatus}"
                                    Visibility="{Binding IsValid, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <ui:TextBlock
                                    Foreground="Red"
                                    Text="{DynamicResource ShareEdit_Text_InvalidStatus}"
                                    Visibility="{Binding IsValid, Converter={StaticResource InvertedBooleanToVisibilityConverter}}" />
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Width="Auto" Header="{DynamicResource ShareEdit_DataGrid_Operation}">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ui:Button
                                Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                CommandParameter="{Binding}"
                                Content="{DynamicResource ShareEdit_Button_Delete}"
                                Icon="{ui:SymbolIcon Symbol=Delete24}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </ui:DataGrid.Columns>
        </ui:DataGrid>
    </Grid>
</ui:FluentWindow>
