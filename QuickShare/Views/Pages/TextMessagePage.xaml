<Page
    x:Class="QuickShare.Views.Pages.TextMessagePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:QuickShare.Helpers"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:QuickShare.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ui:NavigationView.HeaderContent="查看消息"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    ScrollViewer.CanContentScroll="False"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,12,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <StackPanel
                Grid.Column="0"
                HorizontalAlignment="Left"
                Orientation="Horizontal">
                <ui:TextBlock Text="发送者：" />
                <ui:TextBlock Foreground="Green" Text="{Binding Sender}" />
            </StackPanel>
            <StackPanel
                Grid.Column="1"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <ui:Button
                    Command="{Binding CopyTextCommand}"
                    Content="复制"
                    Icon="{ui:SymbolIcon Symbol=Copy24}" />
                <ui:Button
                    Margin="12,0,0,0"
                    Command="{Binding ExportTxtCommand}"
                    Content="导出 .txt">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Foreground="Red" Symbol="ArrowExportLtr24" />
                    </ui:Button.Icon>
                </ui:Button>
                <ui:Button
                    Margin="12,0,0,0"
                    Command="{Binding ExitTextMessageCommand}"
                    Content="返回">
                    <ui:Button.Icon>
                        <ui:SymbolIcon Foreground="Green" Symbol="ArrowLeft24" />
                    </ui:Button.Icon>
                </ui:Button>
            </StackPanel>
        </Grid>
        <ui:TextBox
            Grid.Row="1"
            Margin="0,0,0,24"
            AcceptsReturn="True"
            Background="Transparent"
            BorderBrush="{DynamicResource TextFillColorPrimaryBrush}"
            ClearButtonEnabled="False"
            Text="{Binding Message}"
            TextWrapping="Wrap" />
    </Grid>
</Page>
