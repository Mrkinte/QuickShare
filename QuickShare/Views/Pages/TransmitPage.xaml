<Page
    x:Class="QuickShare.Views.Pages.TransmitPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:QuickShare.Helpers"
    xmlns:local="clr-namespace:QuickShare.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="TransmitPage"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">
    <Page.Resources>
        <helpers:InvertedBooleanToVisibilityConverter x:Key="InvertedBooleanToVisibilityConverter" />
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="4*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ui:Button
                Grid.Column="0"
                Command="{Binding EditAutomaticSortingCommand}"
                Content="自动分类"
                ToolTip="上传文件时，根据文件类型自动保存到对应文件夹中。"
                ToolTipService.InitialShowDelay="0">
                <ui:Button.Icon>
                    <ui:SymbolIcon Foreground="Orange" Symbol="ApprovalsApp24" />
                </ui:Button.Icon>
            </ui:Button>
            <ui:TextBlock
                Grid.Column="1"
                HorizontalAlignment="Center"
                FontSize="24"
                FontWeight="Bold"
                Text="扫描下方二维码下载/上传文件" />
            <ui:Button
                Grid.Column="2"
                HorizontalAlignment="Right"
                Command="{Binding OpenUploadFolderCommand}"
                Content="上传目录"
                ToolTip="打开上传目录"
                ToolTipService.InitialShowDelay="0">
                <ui:Button.Icon>
                    <ui:SymbolIcon Foreground="Orange" Symbol="Folder24" />
                </ui:Button.Icon>
            </ui:Button>
        </Grid>
        <ui:Image
            Grid.Row="1"
            Width="200"
            Height="200"
            Margin="0,12,0,12"
            HorizontalAlignment="Center"
            Source="{Binding QrCodeImage}" />
        <StackPanel
            Grid.Row="2"
            Margin="0,12,0,12"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <ui:HyperlinkButton
                Padding="12,8,12,8"
                Content="{Binding Url}"
                NavigateUri="{Binding Url}" />
            <ui:Button
                Padding="8"
                Background="Transparent"
                BorderThickness="0"
                Command="{Binding CopyUrlCommand}"
                Icon="{ui:SymbolIcon Symbol=Copy24}" />
        </StackPanel>
        <StackPanel
            Grid.Row="3"
            Margin="0,12,0,12"
            HorizontalAlignment="Center"
            Orientation="Horizontal"
            Visibility="{Binding EnableMdns, Converter={StaticResource InvertedBooleanToVisibilityConverter}}">
            <ui:TextBlock VerticalAlignment="Center" Text="请选择直连IP" />
            <ComboBox
                MinWidth="200"
                Margin="8,0,0,0"
                DisplayMemberPath="DisplayString"
                ItemsSource="{Binding ActivedNetworks}"
                SelectedItem="{Binding SelectedNetwork}" />
            <ui:Button
                Margin="4,0,0,0"
                Padding="8"
                Background="Transparent"
                BorderThickness="0"
                Command="{Binding RefreshNetworkCommand}"
                Icon="{ui:SymbolIcon Symbol=ArrowSync24}" />
        </StackPanel>
        <StackPanel
            Grid.Row="4"
            Margin="0,12,0,12"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <ui:TextBlock VerticalAlignment="Center" Text="通过mDNS连接" />
            <ui:ToggleSwitch
                Margin="8,0,0,0"
                Command="{Binding EnableMdnsCommand}"
                IsChecked="{Binding EnableMdns}"
                ToolTip="启用后将通过 计算机名.local 访问文件服务器，mDNS连接失败时请尝试IP直连。"
                ToolTipService.InitialShowDelay="0" />
        </StackPanel>
        <ui:TextBlock
            Grid.Row="5"
            Margin="0,12,0,48"
            HorizontalAlignment="Center"
            FontSize="16"
            FontWeight="Bold"
            Foreground="OrangeRed"
            Text="请确保手机（平板）与电脑在同一局域网内" />
    </Grid>
</Page>
