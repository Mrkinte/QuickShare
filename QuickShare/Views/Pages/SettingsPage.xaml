<Page
    x:Class="QuickShare.Views.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:helpers="clr-namespace:QuickShare.Helpers"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:local="clr-namespace:QuickShare.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sc="clr-namespace:QuickShare.Controls"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="SettingsPage"
    d:DataContext="{d:DesignInstance local:SettingsPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="1800"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">
    <Page.Resources>
        <helpers:InvertedBooleanToVisibilityConverter x:Key="InvertedBooleanToVisibilityConverter" />
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Page.Resources>

    <StackPanel Margin="0,0,0,24">
        <!--  General Settings  -->
        <ui:Card>
            <ui:Card.Style>
                <Style BasedOn="{StaticResource {x:Type ui:Card}}" TargetType="ui:Card">
                    <Setter Property="Border.CornerRadius" Value="8" />
                </Style>
            </ui:Card.Style>
            <StackPanel>
                <TextBlock
                    Margin="12,0,0,0"
                    FontSize="20"
                    Text="通用" />
                <sc:SettingCard
                    Title="开机自启动"
                    Description="开机自动运行软件和文件服务器"
                    Icon="{ui:SymbolIcon Symbol=Laptop24}">
                    <ui:ToggleSwitch Command="{Binding AutoStartupCommand}" IsChecked="{Binding AutoStartup, Mode=TwoWay}" />
                </sc:SettingCard>
                <sc:SettingCard
                    Title="主题"
                    Description="调整应用的外观"
                    Icon="{ui:SymbolIcon Symbol=PaintBrush24}">
                    <ComboBox
                        MinWidth="200"
                        ItemsSource="{Binding ThemeItems}"
                        SelectedItem="{Binding SelectedTheme}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <i:InvokeCommandAction Command="{Binding ChangeThemeCommand}" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </ComboBox>
                </sc:SettingCard>
                <sc:SettingCard
                    Title="自动检查更新"
                    Description="软件运行时自动检查是否存在新版本"
                    Icon="{ui:SymbolIcon Symbol=GlobeSurface24}">
                    <ui:ToggleSwitch Command="{Binding AutoCheckUpdateCommand}" IsChecked="{Binding AutoCheckUpdate, Mode=TwoWay}" />
                </sc:SettingCard>
                <sc:SettingCard
                    Title="禁用关闭提示"
                    Description="关闭窗口时弹窗提示用户选择是最小化还是直接退出"
                    Icon="{ui:SymbolIcon Symbol=ClosedCaptionOff24}">
                    <ui:ToggleSwitch Command="{Binding DisableCloseMessageCommand}" IsChecked="{Binding DisableCloseMessage, Mode=TwoWay}" />
                </sc:SettingCard>
            </StackPanel>
        </ui:Card>

        <!--  Network Settings  -->
        <ui:Card Margin="0,12,0,0">
            <ui:Card.Style>
                <Style BasedOn="{StaticResource {x:Type ui:Card}}" TargetType="ui:Card">
                    <Setter Property="Border.CornerRadius" Value="8" />
                </Style>
            </ui:Card.Style>
            <StackPanel>
                <TextBlock
                    Margin="12,0,0,0"
                    FontSize="20"
                    Text="网络" />
                <sc:SettingCard
                    Title="服务器"
                    Description="运行、停止文件服务器"
                    Icon="{ui:SymbolIcon Symbol=Server24}">
                    <ui:Card
                        Width="100"
                        Padding="0,8,0,8"
                        Cursor="Arrow">
                        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                            <ui:Button
                                Padding="8,6,8,6"
                                Appearance="Success"
                                BorderThickness="0"
                                Command="{Binding StartWebServerCommand}"
                                Icon="{ui:SymbolIcon Symbol=Play24}"
                                IsEnabled="{Binding WebIsStopped}" />
                            <ui:Button
                                Margin="12,0,0,0"
                                Padding="8,6,8,6"
                                Appearance="Danger"
                                BorderThickness="0"
                                Command="{Binding StopWebServerCommand}"
                                Icon="{ui:SymbolIcon Symbol=Stop24}"
                                IsEnabled="{Binding WebIsRunning}" />
                        </StackPanel>
                    </ui:Card>
                </sc:SettingCard>
                <sc:SettingCard
                    Title="端口"
                    Description="服务器访问端口"
                    Icon="{ui:SymbolIcon Symbol=CalendarShield24}">
                    <StackPanel Orientation="Horizontal">
                        <ui:Button
                            Padding="8"
                            Background="Transparent"
                            BorderThickness="0"
                            Command="{Binding ModifyWebPortCommand}"
                            Icon="{ui:SymbolIcon Symbol=Checkmark24}"
                            Visibility="{Binding PortConfirmBtnEnabled, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <ui:NumberBox
                            MinWidth="100"
                            Margin="4,0,0,0"
                            ClearButtonEnabled="False"
                            Maximum="65535"
                            Minimum="1"
                            SpinButtonPlacementMode="Hidden"
                            Text="{Binding WebPort, Mode=TwoWay}"
                            TextAlignment="Center"
                            Value="{Binding WebPort, Mode=TwoWay}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="TextChanged">
                                    <i:InvokeCommandAction Command="{Binding WebPortChangedCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </ui:NumberBox>
                    </StackPanel>
                </sc:SettingCard>
                <sc:SettingCard
                    Title="默认网卡"
                    Description="生成链接时默认调用的网卡"
                    Icon="{ui:SymbolIcon Symbol=NetworkAdapter16}">
                    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                        <ui:Button
                            Margin="0,0,8,0"
                            Padding="8"
                            Background="Transparent"
                            BorderThickness="0"
                            Command="{Binding RefreshNetworkCommand}"
                            Icon="{ui:SymbolIcon Symbol=ArrowSync24}" />
                        <ComboBox
                            MinWidth="200"
                            DisplayMemberPath="DisplayString"
                            ItemsSource="{Binding ActivedNetworks}"
                            SelectedItem="{Binding SelectedNetwork}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="SelectionChanged">
                                    <i:InvokeCommandAction Command="{Binding ChangeNetworkCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </ComboBox>
                    </StackPanel>
                </sc:SettingCard>
            </StackPanel>
        </ui:Card>

        <!--  Transmit Settings  -->
        <ui:Card Margin="0,12,0,0">
            <ui:Card.Style>
                <Style BasedOn="{StaticResource {x:Type ui:Card}}" TargetType="ui:Card">
                    <Setter Property="Border.CornerRadius" Value="8" />
                </Style>
            </ui:Card.Style>
            <StackPanel>
                <TextBlock
                    Margin="12,0,0,0"
                    FontSize="20"
                    Text="传输" />
                <sc:SettingCard
                    Title="登录密码"
                    Description="打开 上传/下载 页面时需要输入的登录密码"
                    Icon="{ui:SymbolIcon Symbol=Password24}">
                    <StackPanel Orientation="Horizontal">
                        <ui:Button
                            Padding="8"
                            Background="Transparent"
                            BorderThickness="0"
                            Command="{Binding ResetPasswordCommand}"
                            Icon="{ui:SymbolIcon Symbol=Checkmark24}"
                            Visibility="{Binding PasswordConfirmBtnEnabled, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <ui:PasswordBox
                            MinWidth="200"
                            Margin="4,0,0,0"
                            MaxLength="16"
                            MaxLines="1"
                            Text="{Binding Password}"
                            ToolTip="支持字母、数字，4-16个字符长度。"
                            ToolTipService.InitialShowDelay="0">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="TextChanged">
                                    <i:InvokeCommandAction Command="{Binding PasswordChangedCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </ui:PasswordBox>
                    </StackPanel>
                </sc:SettingCard>
                <sc:SettingCard
                    Title="文件上传限制"
                    Description="最大允许上传的文件总大小 (MB)"
                    Icon="{ui:SymbolIcon Symbol=Database24}">
                    <StackPanel Orientation="Horizontal">
                        <ui:Button
                            Padding="8"
                            Background="Transparent"
                            BorderThickness="0"
                            Command="{Binding ModifyMaxFileSizeCommand}"
                            Icon="{ui:SymbolIcon Symbol=Checkmark24}"
                            Visibility="{Binding MaxFileSizeConfirmBtnEnabled, Converter={StaticResource BooleanToVisibilityConverter}}" />
                        <ui:NumberBox
                            MinWidth="100"
                            Margin="4,0,0,0"
                            ClearButtonEnabled="False"
                            Cursor="Arrow"
                            Maximum="65535"
                            Minimum="1"
                            SpinButtonPlacementMode="Hidden"
                            Text="{Binding MaxFileSize, Mode=TwoWay}"
                            TextAlignment="Center"
                            Value="{Binding MaxFileSize, Mode=TwoWay}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="TextChanged">
                                    <i:InvokeCommandAction Command="{Binding MaxFileSizeChangedCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </ui:NumberBox>
                    </StackPanel>
                </sc:SettingCard>
                <sc:SettingCard
                    Title="上传文件保存路径"
                    Description="{Binding FileSavePath}"
                    Icon="{ui:SymbolIcon Symbol=Folder24}">
                    <ui:Button
                        MinWidth="100"
                        Command="{Binding SelectFileSavePathCommand}"
                        Content="选择文件夹" />
                </sc:SettingCard>

            </StackPanel>
        </ui:Card>

        <!--  Other Settings  -->
        <ui:Card Margin="0,12,0,0">
            <ui:Card.Style>
                <Style BasedOn="{StaticResource {x:Type ui:Card}}" TargetType="ui:Card">
                    <Setter Property="Border.CornerRadius" Value="8" />
                </Style>
            </ui:Card.Style>
            <StackPanel>
                <TextBlock
                    Margin="12,0,0,0"
                    FontSize="20"
                    Text="其他" />
                <sc:SettingCard
                    Title="查看日志"
                    Description="查看软件运行日志"
                    Icon="{ui:SymbolIcon Symbol=DocumentTextExtract24}">
                    <ui:Button Command="{Binding OpenLogFolderCommand}" Content="打开" />
                </sc:SettingCard>

                <!--  Restore default settings  -->
                <ui:CardControl
                    Background="Transparent"
                    BorderThickness="0"
                    Icon="{ui:SymbolIcon Symbol=Delete24}">
                    <ui:CardControl.Header>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <ui:TextBlock
                                Grid.Row="0"
                                Margin="0,0,0,5"
                                FontSize="16"
                                FontTypography="Body"
                                Text="恢复默认设置" />
                            <ui:TextBlock
                                Grid.Row="1"
                                FontSize="12"
                                Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                                Text="恢复所有参数到默认状态并重启软件" />
                        </Grid>
                    </ui:CardControl.Header>
                    <ui:Button
                        Name="RestoreConfigBtn"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Top"
                        Click="RestoreConfigBtn_Click"
                        Content="恢复" />
                </ui:CardControl>
                <Popup
                    x:Name="RestoreConfigPopup"
                    Height="60"
                    AllowsTransparency="True"
                    Placement="Bottom"
                    PlacementTarget="{Binding ElementName=RestoreConfigBtn}"
                    StaysOpen="False"
                    VerticalOffset="3">
                    <Border Background="{DynamicResource ApplicationBackgroundBrush}" CornerRadius="8">
                        <StackPanel Orientation="Horizontal">
                            <ui:TextBlock
                                Margin="10"
                                VerticalAlignment="Center"
                                Text="软件将重启，确定恢复吗？" />
                            <ui:Button
                                Margin="0,0,10,0"
                                Appearance="Danger"
                                Click="RestoreConfigBtn_Click"
                                Command="{Binding RestoreDefaultCommand}"
                                Content="确认" />
                        </StackPanel>
                    </Border>
                </Popup>

                <sc:SettingCard
                    Title="获取最新版本"
                    Description="{Binding AppVersion}"
                    Icon="{ui:SymbolIcon Symbol=PhoneUpdate24}">
                    <ui:Button Command="{Binding CheckUpdateCommand}" Content="检查更新" />
                </sc:SettingCard>
                <sc:SettingCard
                    Title="问题反馈"
                    Description="在Github中反馈遇到的Bug"
                    Icon="{ui:SymbolIcon Symbol=Bug24}">
                    <ui:HyperlinkButton
                        Appearance="Primary"
                        Content="问题反馈"
                        NavigateUri="https://github.com/Mrkinte/QuickShare/issues" />
                </sc:SettingCard>
            </StackPanel>
        </ui:Card>
    </StackPanel>
</Page>
